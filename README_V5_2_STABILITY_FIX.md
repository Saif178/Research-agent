# v5.2 Stability Fix

Fixes Streamlit Arrow serialization for heterogeneous `step`/dataset columns, replaces deprecated `use_container_width`, adds the missing OpenAI `complete()` compatibility method, and reduces synthesis evidence to 8 excerpts of 900 characters.
